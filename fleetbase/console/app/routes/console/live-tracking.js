import Route from '@ember/routing/route';

/**
 * Route for the live tracking (Traccar) connector page.
 * Added as a standalone route -- does not modify any existing route.
 */
export default class ConsoleLiveTrackingRoute extends Route {
    title = 'Live Tracking';
}
